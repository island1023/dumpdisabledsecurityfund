<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dumpdisabledsecurityfund.mapper.EmployeeRegistrationMapper">

    <resultMap id="BaseResultMap" type="com.example.dumpdisabledsecurityfund.entity.EmployeeRegistration">
        <id column="id" property="id"/>
        <result column="company_id" property="companyId"/>
        <result column="year" property="year"/>
        <result column="name" property="name"/>
        <result column="id_card" property="idCard"/>
        <result column="is_disabled" property="isDisabled"/>
        <result column="disability_cert_no" property="disabilityCertNo"/>
        <result column="job_position" property="jobPosition"/>
        <result column="entry_date" property="entryDate"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO employee_registration(
            company_id, year, name, id_card, is_disabled,
            disability_cert_no, job_position, entry_date, status,
            create_time, update_time
        ) VALUES(
                    #{companyId}, #{year}, #{name}, #{idCard}, #{isDisabled},
                    #{disabilityCertNo}, #{jobPosition}, #{entryDate}, #{status},
                    #{createTime}, #{updateTime}
                )
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM employee_registration WHERE id = #{id}
    </select>

    <select id="selectByCompanyId" resultMap="BaseResultMap">
        SELECT * FROM employee_registration
        WHERE company_id = #{companyId}
        ORDER BY id DESC
    </select>

    <select id="selectByCompanyIdAndYear" resultMap="BaseResultMap">
        SELECT * FROM employee_registration
        WHERE company_id = #{companyId} AND year = #{year}
        ORDER BY id DESC
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT * FROM employee_registration
        WHERE status = #{status}
        ORDER BY create_time DESC
    </select>

    <select id="selectByIdCard" resultMap="BaseResultMap">
        SELECT * FROM employee_registration WHERE id_card = #{idCard}
    </select>

    <update id="updateById">
        UPDATE employee_registration
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="idCard != null">id_card = #{idCard},</if>
            <if test="isDisabled != null">is_disabled = #{isDisabled},</if>
            <if test="disabilityCertNo != null">disability_cert_no = #{disabilityCertNo},</if>
            <if test="jobPosition != null">job_position = #{jobPosition},</if>
            <if test="entryDate != null">entry_date = #{entryDate},</if>
            <if test="status != null">status = #{status},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE employee_registration
        SET status = #{status}, update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM employee_registration WHERE id = #{id}
    </delete>

    <delete id="deleteByCompanyIdAndYear">
        DELETE FROM employee_registration
        WHERE company_id = #{companyId} AND year = #{year}
    </delete>

</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dumpdisabledsecurityfund.mapper.CompanyUserMapper">

    <resultMap id="BaseResultMap" type="com.example.dumpdisabledsecurityfund.entity.CompanyUser">
        <id column="id" property="id"/>
        <result column="company_id" property="companyId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="mobile" property="mobile"/>
        <result column="email" property="email"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO company_user(
            company_id, username, password, name, mobile, email,
            status, create_time, update_time
        ) VALUES(
                    #{companyId}, #{username}, #{password}, #{name}, #{mobile}, #{email},
                    #{status}, #{createTime}, #{updateTime}
                )
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM company_user WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM company_user WHERE username = #{username} LIMIT 1
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM company_user
        ORDER BY id DESC
    </select>

    <select id="selectByCompanyId" resultMap="BaseResultMap">
        SELECT * FROM company_user
        WHERE company_id = #{companyId}
        ORDER BY id DESC
    </select>

    <select id="selectByCompanyIdAndStatus" resultMap="BaseResultMap">
        SELECT * FROM company_user
        WHERE company_id = #{companyId} AND status = #{status}
        ORDER BY id DESC
    </select>

    <update id="updateById">
        UPDATE company_user
        <set>
            <if test="password != null">password = #{password},</if>
            <if test="name != null">name = #{name},</if>
            <if test="mobile != null">mobile = #{mobile},</if>
            <if test="email != null">email = #{email},</if>
            <if test="status != null">status = #{status},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateLastLoginTime">
        UPDATE company_user
        SET update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE company_user
        SET password = #{password}, update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM company_user WHERE id = #{id}
    </delete>

    <delete id="deleteByCompanyId">
        DELETE FROM company_user WHERE company_id = #{companyId}
    </delete>

</mapper>


<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dumpdisabledsecurityfund.mapper.UserRoleMapper">

    <resultMap id="BaseResultMap" type="com.example.dumpdisabledsecurityfund.entity.UserRole">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="role_id" property="roleId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_role(user_id, role_id, create_time)
        VALUES(#{userId}, #{roleId}, #{createTime})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM user_role WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM user_role WHERE user_id = #{userId}
    </select>

    <select id="selectRoleCodesByUserId" resultType="string">
        SELECT r.role_code
        FROM user_role ur
                 INNER JOIN role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
    </select>

    <select id="selectRoleIdsByUserId" resultType="long">
        SELECT role_id FROM user_role WHERE user_id = #{userId}
    </select>

    <select id="selectByRoleId" resultMap="BaseResultMap">
        SELECT * FROM user_role WHERE role_id = #{roleId}
    </select>

    <insert id="batchInsert">
        INSERT INTO user_role(user_id, role_id, create_time) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.roleId}, #{item.createTime})
        </foreach>
    </insert>

    <delete id="deleteByUserId">
        DELETE FROM user_role WHERE user_id = #{userId}
    </delete>

    <delete id="deleteByUserIdAndRoleId">
        DELETE FROM user_role
        WHERE user_id = #{userId} AND role_id = #{roleId}
    </delete>

    <delete id="deleteByRoleId">
        DELETE FROM user_role WHERE role_id = #{roleId}
    </delete>

</mapper>
